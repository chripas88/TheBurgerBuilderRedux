import React from 'react';
import classes from '../../stylesheets/components/ui/Spinner.module.css';

const spinner = () => (
	<div className={classes.Loader}>Loading...</div>
);

export default spinner;